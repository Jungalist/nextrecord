<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.szaman.nextrecord">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.example.szaman.nextrecord.LookingForRecord"
            android:label="@string/title_looking_for_record"
            android:parentActivityName="com.example.szaman.nextrecord.MainMenu" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.szaman.nextrecord.LookingForRecord" />
        </activity>
    </application>

</manifest>